/* 
	Automatically register and run all modules in App.Core.views 
	Use App.Core.register() to manually register modules
	Use App.Core.start() to manually start one module
	App.Core.registerAll() to register all and App.Core.startAll() to start all
	App.run('your-app-selector') combines registerAll and startAll
*/

App.run('#my-app');
